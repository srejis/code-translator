# AGENTS.md

## Project Instructions

- Read and write all text files as UTF-8.
- Use the `code_trans` Python environment for this project.
- The Python environment is expected to be under `D:\anaconda3`.
- On Windows PowerShell, prefer commands that explicitly set UTF-8 when reading or writing files, for example `Get-Content -Encoding UTF8`.
